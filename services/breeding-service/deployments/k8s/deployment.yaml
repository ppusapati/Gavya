apiVersion: apps/v1
kind: Deployment
metadata:
  name: breeding-service
  labels:
    app: breeding-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: breeding-service
  template:
    metadata:
      labels:
        app: breeding-service
    spec:
      containers:
      - name: breeding-service
        image: gavya/breeding-service:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: breeding-service-config
        - secretRef:
            name: breeding-service-secret
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: breeding-service
spec:
  selector:
    app: breeding-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: breeding-service-config
data:
  SERVICE_NAME: breeding-service
  SERVER_ADDR: ":8080"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: breeding-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: breeding-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
