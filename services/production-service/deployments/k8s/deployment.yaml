apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-service
  labels:
    app: production-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: production-service
  template:
    metadata:
      labels:
        app: production-service
    spec:
      containers:
      - name: production-service
        image: gavya/production-service:latest
        ports:
        - containerPort: 8107
        envFrom:
        - configMapRef:
            name: production-service-config
        - secretRef:
            name: production-service-secret
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8107
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8107
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
